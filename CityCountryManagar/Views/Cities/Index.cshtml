@using RidersApp.ViewModels
@{
    ViewData["Title"] = "Cities";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-2">
        <h4 class="mb-0">Cities</h4>
        <div>
            <button class="btn btn-success" onclick="showInPopup('@Url.Action("AddOrEdit", "Cities")', 'Add City')">Add New</button>
        </div>
    </div>
    <div id="view-all">
        @await Html.PartialAsync("_ViewAll", new List<CityVM>())
    </div>
</div>

@* Add anti-forgery token for AJAX requests *@
@Html.AntiForgeryToken()

@section Scripts {
    <script>
        $(document).ready(function() {
            // Load cities on page load
            $.get('@Url.Action("GetCities", "Cities")', function(data) {
                $("#view-all").html(data);
            });
        });
    </script>
}
